<h1> Welcome <%= @users.name %></h1>
<ul>
  <% @users.each do |user| %>
    <li> <%= link_to user.username, users_index_url(user) %> </li>
  <% end %>
</ul>

<%= button_to "See your requests", '/requests/index', method: :get %> 
