

<div> <% @listings.each do |listing|  %>
       <div><%= link_to listing.title, listing_path(listing) %></div>
       <% end %>
</div>


<%= button_to "New Listing", { action: "new" }, method: :get, class: "my_class" %>
